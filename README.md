# jailrun-hub

A curated collection of versioned Ansible playbooks for jailrun (`jrun`): runtimes, databases, and services.

Each playbook directory includes a `playbook.yml` entrypoint and a `jrun.manifest` with checksums, so `jrun` can download and verify playbooks.
